<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="batis.HelloFromSQL">
    <select id="selectData" resultType="batis.HelloFromSQL">
        select * from TEST where id = #{id}
    </select>
    <select id="selectAllData" resultType="batis.HelloFromSQL">
        select * from TEST
    </select>
</mapper>